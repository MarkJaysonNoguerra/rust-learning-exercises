fn main() {
    // first example
    // panic!("crash and burn");

    // Second example
    // Using [] is supposed to return an element, but if you
    // pass an invalid index, rust will panic!
    // let v = vec![1, 2, 3];
    // v[99];
}
